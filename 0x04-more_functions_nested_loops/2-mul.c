#include"main.h"
/**
 * mul - multiplies two integers
 * @a: one of the integers to be multiplied
 * @b: one of the integer to be multiplied
 *
 * Return: returns the result of the multiplication
 */
int mul(int a, int b)
{
	int result;

	result = (a * b);
	return (result);
}
