#include"main.h"
#include<stdio.h>

/**
 * main - theis is main function
 * @argc: it counts the number of arguments
 * @argv: this is a array of paramenters
 *
 * Return: always 0.
 */

int main(int __attribute__((__unused__))argc, char *argv[])
{
	printf("The name of the prog is: %s\n", argv[0]);
	return (0);
}
